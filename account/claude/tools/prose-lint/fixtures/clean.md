# Scanner Coverage

The scanner runs on every pull request and posts results to the security tab. Three repos still lack the workflow file. Add it there first; the rest inherit coverage from the org config.
